import { NextResponse } from 'next/server';

// TODO: Implement cart API routes
export async function GET() {
  return NextResponse.json({ message: 'Cart API endpoint' });
}

export async function POST() {
  return NextResponse.json({ message: 'Cart API endpoint' });
}
