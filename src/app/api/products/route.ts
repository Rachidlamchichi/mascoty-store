import { NextResponse } from 'next/server';

// TODO: Implement products API routes
export async function GET() {
  return NextResponse.json({ message: 'Products API endpoint' });
}

export async function POST() {
  return NextResponse.json({ message: 'Products API endpoint' });
}
